import { Alert } from '@mui/material';
import React from 'react';
import PageLayout from '../../components/PageLayout/PageLayout';

const Profile = () => {
  return (
    <PageLayout>
      <Alert severity="info">This page is not just built yet.</Alert>
    </PageLayout>
  );
};

export default Profile;
